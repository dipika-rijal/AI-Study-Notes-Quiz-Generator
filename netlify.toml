[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — required for React Router (fixes 404 on page refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"